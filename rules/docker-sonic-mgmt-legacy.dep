
DPATH       := $($(DOCKER_SONIC_MGMT_LEGACY)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-sonic-mgmt-legacy.mk rules/docker-sonic-mgmt-legacy.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_SONIC_MGMTDOCKER_SONIC_MGMT_LEGACY)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_SONIC_MGMTDOCKER_SONIC_MGMT_LEGACY)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SONIC_MGMTDOCKER_SONIC_MGMT_LEGACY)_DEP_FILES   := $(DEP_FILES)

